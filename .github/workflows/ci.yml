name: CI Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Simulate Build
        run: docker compose up -d --build
             docker logs devoppppppe2-db-1


      - name: Simulate Test
        run: curl http://lvm66097.sit.kmutt.ac.th:5000
